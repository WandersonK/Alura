function minhaFuncao(param) {
    // Bloco de código
}

// minhaFuncao(param);

// expressão de função

// diferença principal da função "soma" e "minhaFuncao": HOISTING
// funções e var são "Listadas" no topo do arquivo.

// console.log(soma(1, 1)); // Aqui a função soma não será executada

const soma = function(num1, num2) { return num1 + num2 }

// console.log(soma(1, 2)); // Aqui a função soma é executada